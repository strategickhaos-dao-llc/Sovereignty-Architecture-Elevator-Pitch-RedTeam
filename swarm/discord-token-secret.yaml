# Discord Token Secret - TEMPLATE
# NOTE: This is a TEMPLATE file. DO NOT commit actual tokens to version control.
#
# Usage:
#   1. Copy this file to a local, untracked location
#   2. Replace the placeholder token with your actual Discord bot token
#   3. Apply with: kubectl apply -f /path/to/your/local/secret.yaml
#
# For production, use external secret management:
#   - HashiCorp Vault
#   - GCP Secret Manager
#   - Kubernetes External Secrets Operator
#
apiVersion: v1
kind: Secret
metadata:
  name: discord-token
  namespace: default
  labels:
    app: swarm
    component: secrets
type: Opaque
stringData:
  # IMPORTANT: Replace with your actual Discord bot token before deployment
  # Never commit real tokens to version control
  token: "REPLACE_WITH_YOUR_DISCORD_BOT_TOKEN"
