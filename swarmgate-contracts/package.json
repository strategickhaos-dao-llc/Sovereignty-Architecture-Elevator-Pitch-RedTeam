{
  "name": "swarmgate-contracts",
  "version": "1.0.0",
  "description": "Immutable 7% Charity Splitter for SwarmGate",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  }
}
