# Secrets & Security

All secrets (Discord token, GitHub private key, HMAC keys, API keys) are injected at runtime via Kubernetes Secrets / HashiCorp Vault / Sealed Secrets.

**Zero secrets are committed to this repository.**
