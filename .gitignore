# Generated by Cargo
# will have compiled files and executables
debug
target

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
node_modules

# Java build artifacts
*.class
*.jar
*.war
*.ear
target/
.mvn/
*.log

# JDK installations
.local/jdks/

# Workspace data
workspace/

# ========================================
# API Keys & Secrets Security
# ========================================
# CRITICAL: Never commit API keys or secrets
# xAI / Grok API keys
xai-*
*xai-key*
*grok*key*

# Environment files with real credentials
.env
.env.local
.env.*.local
.env.production
.env.staging

# API key files (any format)
api_key*
*api*key*
*apikey*
*.key
*.pem
*.p12
*.pfx

# Secret management files
secrets/
secret/
.secrets/
*secret*.json
*secret*.yaml
*secret*.yml
*credentials*.json

# Password and token files
*password*
*token*.txt
*bearer*.txt

# Screenshots directory that might contain sensitive data
screenshots/
*screenshot*.png
*screenshot*.jpg
*screenshot*.jpeg
# Block temporary or debug images
temp/*.png
temp/*.jpg
tmp/*.png
tmp/*.jpg
# Note: Project images in docs/, assets/, and root are allowed

# Bitwarden/1Password exports
*export*.csv
*vault*export*

# Cloud provider credentials
.aws/
.azure/
.gcloud/
*credentials*

# SSH keys
*.pub
id_rsa*
id_ed25519*
id_ecdsa*
