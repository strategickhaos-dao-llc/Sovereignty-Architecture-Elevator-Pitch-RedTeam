table inet swarmsg {
  set caps { type ipv4_addr; flags interval; }
  chain preraw {
    type filter hook prerouting priority -300; policy accept;
    # Example: mark NATS traffic from authorized peers
    ip saddr @caps tcp dport { 4222, 6222 } accept
  }
}
