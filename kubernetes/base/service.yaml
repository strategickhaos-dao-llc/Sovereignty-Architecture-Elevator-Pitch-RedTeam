apiVersion: v1
kind: Service
metadata:
  name: sovereignty-core
  namespace: sovereignty-system
spec:
  selector:
    app: sovereignty-core
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
