apiVersion: apps/v1
kind: Deployment
metadata:
  name: sovereignty-core
spec:
  replicas: 5
  template:
    spec:
      containers:
      - name: core
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
